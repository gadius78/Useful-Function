int IsPrime(int input) // Is it prime number??
{
    int i = 0;
	for( i = 2; i < input; i++)
	{
	   if( (input % i) == 0)
	   {
            return 0;
        }
	}
	return 1;
}

// if return is 1, input is prime number 
